<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %>
<body>
  <%- include('../partials/header') %>

  <main class="section">
    <div class="container detail-layout">
      <div class="detail-img"><%= product.image %></div>
      <section class="panel">
        <p class="muted">Inicio / Productos / <%= product.category ? product.category.name : 'Sin categoría' %></p>
        <h1><%= product.name %></h1>
        <p class="price">$<%= Number(product.price).toLocaleString('es-AR') %></p>
        <p><%= product.description %></p>

        <ul class="features">
          <li>Categoría: <%= product.category ? product.category.name : 'Sin categoría' %></li>
          <li>Marca: <%= product.brand ? product.brand.name : 'Sin marca' %></li>
          <li>Colores: <%= product.colors && product.colors.length ? product.colors.map(color => color.name).join(', ') : 'Sin especificar' %></li>
          <li>Producto destacado: <%= product.featured ? 'Sí' : 'No' %></li>
        </ul>

        <div class="detail-actions">
          <a class="btn btn-secondary" href="/cart">Agregar al carrito</a>
          <a class="btn btn-outline" href="/products/<%= product.id %>/edit">Editar producto</a>
          <form action="/products/<%= product.id %>?_method=DELETE" method="POST" class="inline-form">
            <button class="btn btn-danger" type="submit">Eliminar producto</button>
          </form>
        </div>
      </section>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
